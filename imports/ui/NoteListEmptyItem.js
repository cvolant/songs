import React from 'react';

const NoteListEmptyItem = () => {
    return (
        <p className='empty-item'>No note yet. Click the 'Add note' button to create a first one.</p>
    );
}

export default NoteListEmptyItem;